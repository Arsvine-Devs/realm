diff --git a/dist/events-156d8d12.esm.js b/dist/events-156d8d12.esm.js
index 9681d0c62238d67f2d5ad389307dfd45ee8ba7ba..401aecfcdab79609811ac02c38e812ffac46aefc 100644
--- a/dist/events-156d8d12.esm.js
+++ b/dist/events-156d8d12.esm.js
@@ -993,6 +993,38 @@ const createStore = (invalidate, advance) => {
       }
     }));
     const pointer = new THREE.Vector2();
+    const clock = new THREE.Timer();
+    clock.autoStart = true;
+    clock.running = false;
+    clock.elapsedTime = 0;
+    clock.oldTime = 0;
+    clock.start = () => {
+      clock.reset();
+      clock.running = true;
+      clock.elapsedTime = 0;
+      clock.oldTime = 0;
+    };
+    clock.getDelta = () => {
+      if (clock.autoStart && !clock.running) {
+        clock.start();
+        return 0;
+      }
+      if (!clock.running) return 0;
+      clock.update();
+      const delta = THREE.Timer.prototype.getDelta.call(clock);
+      clock.oldTime = clock.elapsedTime;
+      clock.elapsedTime += delta;
+      return delta;
+    };
+    clock.getElapsedTime = () => {
+      clock.getDelta();
+      return clock.elapsedTime;
+    };
+    clock.stop = () => {
+      clock.getElapsedTime();
+      clock.running = false;
+      clock.autoStart = false;
+    };
     const rootState = {
       set,
       get,
@@ -1013,7 +1045,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE.Clock(),
+      clock,
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/dist/events-4c71f21f.cjs.prod.js b/dist/events-4c71f21f.cjs.prod.js
index 65e702ef67d954387307b168529752690a586fd0..8540f7b5870e939770ef13e3357d835370531858 100644
--- a/dist/events-4c71f21f.cjs.prod.js
+++ b/dist/events-4c71f21f.cjs.prod.js
@@ -1018,6 +1018,38 @@ const createStore = (invalidate, advance) => {
       }
     }));
     const pointer = new THREE__namespace.Vector2();
+    const clock = new THREE__namespace.Timer();
+    clock.autoStart = true;
+    clock.running = false;
+    clock.elapsedTime = 0;
+    clock.oldTime = 0;
+    clock.start = () => {
+      clock.reset();
+      clock.running = true;
+      clock.elapsedTime = 0;
+      clock.oldTime = 0;
+    };
+    clock.getDelta = () => {
+      if (clock.autoStart && !clock.running) {
+        clock.start();
+        return 0;
+      }
+      if (!clock.running) return 0;
+      clock.update();
+      const delta = THREE__namespace.Timer.prototype.getDelta.call(clock);
+      clock.oldTime = clock.elapsedTime;
+      clock.elapsedTime += delta;
+      return delta;
+    };
+    clock.getElapsedTime = () => {
+      clock.getDelta();
+      return clock.elapsedTime;
+    };
+    clock.stop = () => {
+      clock.getElapsedTime();
+      clock.running = false;
+      clock.autoStart = false;
+    };
     const rootState = {
       set,
       get,
@@ -1038,7 +1070,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock,
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/dist/events-b1bdeb1a.cjs.dev.js b/dist/events-b1bdeb1a.cjs.dev.js
index 7785529f79299cdefe706c0df1d5c756817ca274..7affaadfa4edb51d951bba10bfdba1f6e1fc58ce 100644
--- a/dist/events-b1bdeb1a.cjs.dev.js
+++ b/dist/events-b1bdeb1a.cjs.dev.js
@@ -1018,6 +1018,38 @@ const createStore = (invalidate, advance) => {
       }
     }));
     const pointer = new THREE__namespace.Vector2();
+    const clock = new THREE__namespace.Timer();
+    clock.autoStart = true;
+    clock.running = false;
+    clock.elapsedTime = 0;
+    clock.oldTime = 0;
+    clock.start = () => {
+      clock.reset();
+      clock.running = true;
+      clock.elapsedTime = 0;
+      clock.oldTime = 0;
+    };
+    clock.getDelta = () => {
+      if (clock.autoStart && !clock.running) {
+        clock.start();
+        return 0;
+      }
+      if (!clock.running) return 0;
+      clock.update();
+      const delta = THREE__namespace.Timer.prototype.getDelta.call(clock);
+      clock.oldTime = clock.elapsedTime;
+      clock.elapsedTime += delta;
+      return delta;
+    };
+    clock.getElapsedTime = () => {
+      clock.getDelta();
+      return clock.elapsedTime;
+    };
+    clock.stop = () => {
+      clock.getElapsedTime();
+      clock.running = false;
+      clock.autoStart = false;
+    };
     const rootState = {
       set,
       get,
@@ -1038,7 +1070,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock,
       pointer,
       mouse: pointer,
       frameloop: 'always',
